[project]
name = "wthr"
version = "0.1.0"
description = "A terminal-based weather application using OpenWeatherMap API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.31.0",
    "cursor>=1.3.5",
    "geopy>=2.4.0",
]

[project.scripts]
wthr = "wthr:main"
